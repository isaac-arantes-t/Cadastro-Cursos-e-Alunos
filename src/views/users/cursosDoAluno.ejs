<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inscrições de <%= student.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <a href="/users" class="btn btn-primary mt-3">Sair</a>
        <h1>Cursos de <%= student.name %></h1>
        <h2>ID do Aluno: <%= student.student_id %></h2>
        <h4>Cursos Inscritos:</h4>
        <ul class="list-group">
            <% if (courses.length > 0) { %>
                <% courses.forEach(course => { %>
                    <li class="list-group-item"><%= course.name %> - <%= course.duration %> horas</li>
                <% }) %>
            <% } else { %>
                <li class="list-group-item">Este aluno não está inscrito em nenhum curso.</li>
            <% } %>
        </ul>
        
        <a href="/enroll/<%= student.student_id %>" class="btn btn-primary mt-4">Inscrever em um novo curso</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

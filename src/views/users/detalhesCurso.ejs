<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalhes do Curso</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1>Detalhes do Curso: <%= course.name %></h1>
        <p><strong>ID:</strong> <%= course.course_id %></p>
        <p><strong>Descrição:</strong> <%= course.description %></p>
        <p><strong>Duração:</strong> <%= course.duration %></p>
        <p><strong>Valor:</strong> <%= course.price %></p>

        <h2>Alunos Inscritos</h2>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Email</th>
                </tr>
            </thead>
            <tbody>
                <% if (students.length > 0) { %>
                    <% students.forEach(student => { %>
                        <tr>
                            <td>
                                <a href="/enrollments/<%= student.student_id %>"><%= student.name %></a>
                            </td>
                            <td><%= student.email %></td>
                        </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="2">Nenhum aluno inscrito nesse curso.</td>
                    </tr>
                <% } %>
            </tbody>
        </table>

        <a href="/users/list" class="btn btn-primary mt-3">Voltar para Lista de Cursos</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
